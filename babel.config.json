{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": "> 1%, last 2 versions, not ie <= 8",
        "useBuiltIns": "usage", // 可选值 'entry' | 'usage' | false，'usage' 表示按需注入
        "corejs": 3
      }
    ]
  ]
}